import { Routes } from "@angular/router";
import { PrototypeComponent } from "./prototype/prototype.component";

export const prototypeRoutes: Routes = [
    { path: '', component: PrototypeComponent, title: 'Proposal List' }
];
